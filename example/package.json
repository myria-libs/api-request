{
    "name": "example",
    "version": "1.1.0",
    "description": "example use the npm package",
    "main": "dist/index.js",
    "scripts": {
        "test": "echo 'example testing a customized npm package on local'",
        "prebuild": "rm -rf ./dist",
        "build": "tsc",
        "start": "npm run build && node dist/index.js",
        "dev": "npx ts-node src/index.ts"
    },
    "author": "Le Ngoc Duy",
    "license": "MIT",
    "dependencies": {
        "@myria/api-request": "file:..",
        "dotenv": "^16.4.5",
        "dotenv-expand": "^11.0.6"
    },
    "devDependencies": {
        "ts-node-dev": "^2.0.0",
        "tsx": "^4.18.0",
        "typescript": "^5.5.4"
    }
}
